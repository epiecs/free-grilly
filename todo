print_wifi_connection -> local ap settings ook tonen

openapi spec voor settings voorzien - later aan te vullen
openapi spec inladen in postman als test
post van settings voorzien
preferences.h aanpassen -> via grillconfig class -> template class?


network ip opslaan
network ip inladen in main.cpp + functie eens settings saved
  Aparte wifi settings mss voorzien? -> altijd reconnect doen
  indien settings post ssid of ww != stored ww ssid reconnect
  indien settings ip anders ook reconnect

info op bbqgenootschap plaatsen?
licentie bespreken met jeroen/christos?


preferences uitwerken


fix voor openapi spec -> bug logged, zit in upstream renderer

global.h verder correct uitwerken zodat alles er in staat
config.h verder uitwerken en correct comment headers voorzien


grill name generation enzovoort in grill settings duwen

wifi connected variable switchen naargelang status. 
battery percentage enzo ook via globals inlezen -- DONE --

preferences lezen en schrijven


api settings -> fix
set wifi hostname
multicast dns setup

validate json schema -> Schemas.h?
  list loop

generate uuid4 for unique identifier

local ap ssid and password
factory reset settings
default ap is open

how to do event handling?
  if settings are updated how to we run needed functions?

settings page GET/POST
  no wifimulti, if users want to change the ap they should go to the local ap and change the settings
  frontend -> on change wifi try to connect and if successfull return 200 and redirect option

press side button
  tap, <1s    -> show current local ssid + pass + current ssid/ip info
  hold <10s   -> shutdown
  hold >10s   -> factory reset, clear preferences
  which beep signals?

WIFI
hostname : https://randomnerdtutorials.com/esp32-useful-wi-fi-functions-arduino/ -> change esp32 hostname

  // allow to address the device by the given name e.g. http://webserver
  WiFi.setHostname(HOSTNAME);


wifimulti: https://randomnerdtutorials.com/esp32-wifimulti/

favicon


--> https://github.com/espressif/arduino-esp32/blob/master/libraries/WebServer/examples/WebServer/WebServer.ino
--------
// local time zone definition (Berlin)
#define TIMEZONE "CET-1CEST,M3.5.0,M10.5.0/3"
configTzTime(TIMEZONE, "pool.ntp.org");

---

wifi scanner for settings page to scan for wifi + setup

---
multicast dns https://johnwargo.com/posts/2023/arduino-esp32-web-server-on-a-processor-core/#:~:text=Multicast%20DNS

---
generate unique hostname extension based on the last 3 byte of the mac
