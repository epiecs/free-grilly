bootstrap css includen -> if new setup is dit nog niet in cache. wat te doen met filesize

sometimes after bootup a dhcp ip is used instead of the static ip -> race condition?
  maybe delay needed after settings fetch

temp icoon op overzicht ook ander icoon voorzien voor range? evt ++ - __

indien bijna temp -> oranje, indien temp -> groen in html
indien target groen voor er in, oranje voor +-10, rood voor meer als +- 10


hold button to boot? how to with interrupt?

beep on wifi disconnect if in settings

buzzer songs
screen sometimes not booting

wifimulti gebruiken voor overschakeling van 1 ap naar het andere indien settings veranderd worden? -> gaat niet met local ap..

info op bbqgenootschap plaatsen?
licentie bespreken met jeroen/christos?


multicast dns setup


press side button
  tap, <1s    -> show temps / show wifi settings / show local ap settings / show firmware/uuid -> only if alarm not muted?
  hold <10s   -> shutdown
  hold >10s   -> factory reset, clear preferences
  which beep signals?


--> https://github.com/espressif/arduino-esp32/blob/master/libraries/WebServer/examples/WebServer/WebServer.ino
--------
// local time zone definition (Berlin)
#define TIMEZONE "CET-1CEST,M3.5.0,M10.5.0/3"
configTzTime(TIMEZONE, "pool.ntp.org");

---
multicast dns https://johnwargo.com/posts/2023/arduino-esp32-web-server-on-a-processor-core/#:~:text=Multicast%20DNS
